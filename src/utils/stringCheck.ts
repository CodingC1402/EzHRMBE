export interface stringCheckRules {
    allowNumbers: boolean,
    allowFirstCharNumber: boolean,
    bannedCharacters: string[]
}

export function stringCheck(str: string, rules: stringCheckRules) {

}

export function emailCheck(str: string, rules: stringCheckRules) {
    
}